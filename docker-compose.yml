services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: larry
      POSTGRES_PASSWORD: larry
      POSTGRES_DB: larry
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
  pgdata:
  redisdata:
